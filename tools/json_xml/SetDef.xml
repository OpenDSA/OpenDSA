<?xml version="1.0" encoding="utf8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.13.1 -->
<document ids="sets-and-relations" names="sets\ and\ relations" source="&lt;string&gt;" title="Sets and Relations"><title>Sets and Relations</title><comment xml:space="preserve">This file is part of the OpenDSA eTextbook project. See</comment><comment xml:space="preserve">http://algoviz.org/OpenDSA for more details.</comment><comment xml:space="preserve">Copyright (c) 2012-2016 by the OpenDSA Project Contributors, and</comment><comment xml:space="preserve">distributed under an MIT open source license.</comment><raw format="xml" xml:space="preserve"><avmetadata>null</avmetadata></raw><section ids="set-notation" names="set\ notation"><title>Set Notation</title><paragraph>The concept of a set in the mathematical sense has wide
application in computer science.
The notations and techniques of set theory are commonly used
when describing and implementing algorithms because the abstractions
associated with sets often help to clarify and simplify algorithm
design.</paragraph><paragraph>A <title_reference>set</title_reference> is a collection of distinguishable
<title_reference>members &lt;member&gt;</title_reference> or <title_reference>elements &lt;element&gt;</title_reference>.
The members are typically drawn from some larger population known as
the <title_reference>base type</title_reference>.
Each member of a set is either a <title_reference>primitive element</title_reference> of the
base type or is a set itself.
There is no concept of duplication in a set.
Each value from the base type is either in the set or not in the set.
For example, a set named <math>\mathbf{P}</math> might consist of the three
integers 7, 11, and 42.
In this case, <math>\mathbf{P}</math>'s members are 7, 11, and 42, and the
base type is integer.</paragraph><paragraph>The following table shows the symbols commonly used to express sets
and their relationships.</paragraph><target refid="setnotation"></target><topic ids="setnotation" names="setnotation"><title>Table</title><math_block xml:space="preserve">\begin{array}{l|l}
\{1, 4\}&amp; \mbox{A set composed of the members 1 and 4}\\
\{\mathsf{x}\, |\, \mathsf{x}\ \mbox{is a positive integer}\}&amp;
   \mbox{A set definition using a set former}\\
&amp;\qquad \mbox{Example: the set of all positive integers}\\
\mathsf{x} \in \mathbf{P}&amp;\mathsf{x}\ \mbox{is a member of set}\ \mathbf{P}\\
\mathsf{x} \notin \mathbf{P}&amp;\mathsf{x}\ \mbox{is not a member of set}\ \mathbf{P}\\
\emptyset&amp;\mbox{The null or empty set}\\
|\mathbf{P}|&amp; \mbox{Cardinality: size of set}\ \mathbf{P}
           \mbox{or number of members for set}\ \mathbf{P}\\
\mathbf{P}\,\subseteq\,\mathbf{Q},
  \mathbf{Q}\,\supseteq\,\mathbf{P}&amp;
  \mbox{Set}\ \mathbf{P}\ \mbox{is included in set}\ \mathbf{Q},\\
&amp;\qquad \mbox{set}\ \mathbf{P}\ \mbox{is a subset of set}\ \mathbf{Q},\\
&amp;\qquad \mbox{set}\ \mathbf{Q}\ \mbox{is a superset of set}\ \mathbf{P}\\
\mathbf{P}\,\cup\,\mathbf{Q}      &amp;
  \mbox{Set Union: all elements appearing in}
  \ \mathbf{P}\ \mbox{OR}\ \mathbf{Q}\\
\mathbf{P}\,\cap\,\mathbf{Q}      &amp;
  \mbox{Set Intersection: all elements appearing in}\ \mbox{P}
  \ \mbox{AND}\ \mathbf{Q}\\
\mathbf{P}\,-\,\mathbf{Q} &amp;
  \mbox{Set difference: all elements of set}
  \ \mathbf{P}\ \mbox{NOT in set}\ \mathbf{Q}\\
\end{array}</math_block></topic><paragraph>Here are some examples of this notation in use.
First define two sets, <math>\mathbf{P}</math> and <math>\mathbf{Q}</math>.</paragraph><math_block xml:space="preserve">\mathbf{P} = \{2, 3, 5\}, \qquad \mathbf{Q} = \{5, 10\}.</math_block><paragraph><math>|\mathbf{P}| = 3</math>  (because <math>\mathbf{P}</math> has three
members) and <math>|\mathbf{Q}| = 2</math>
(because <math>\mathbf{Q}</math> has two members).
The union of <math>\mathbf{P}</math> and <math>\mathbf{Q}</math>, written
<math>\mathbf{P} \cup \mathbf{Q}</math>, is the set of elements in either
<math>\mathbf{P}</math> or <math>\mathbf{Q}</math>, which is {2, 3, 5, 10}.
The intersection of <math>\mathbf{P}</math> and <math>\mathbf{Q}</math>,
written <math>\mathbf{P} \cap \mathbf{Q}</math>, is the set of elements that
appear in both <math>\mathbf{P}</math> and <math>\mathbf{Q}</math>, which is {5}.
The set difference of <math>\mathbf{P}</math> and <math>\mathbf{Q}</math>,
written <math>\mathbf{P} - \mathbf{Q}</math>,
is the set of elements that occur in <math>\mathbf{P}</math> but not in
<math>\mathbf{Q}</math>, which is {2, 3}.
Note that
<math>\mathbf{P} \cup \mathbf{Q} = \mathbf{Q} \cup \mathbf{P}</math>
and that
<math>\mathbf{P} \cap \mathbf{Q} = \mathbf{Q} \cap \mathbf{P}</math>,
but in general
<math>\mathbf{P} - \mathbf{Q} \neq \mathbf{Q} - \mathbf{P}</math>.
In this example,
<math>\mathbf{Q} - \mathbf{P}  = \{10\}</math>.
Finally, the set {5, 3, 2} is indistinguishable from set
<math>\mathbf{P}</math>, because sets have no concept of order.
Likewise, set {2, 3, 2, 5} is also indistinguishable from
<math>\mathbf{P}</math>, because sets have no concept of duplicate elements.</paragraph><paragraph>The <title_reference>powerset</title_reference> of a set <math>\mathbf{S}</math> is the set of all
possible subsets for <math>\mathbf{S}</math>.
Consider the set <math>\mathbf{S} = \{ a, b, c \}</math>.
The powerset of <math>\mathbf{S}</math> is</paragraph><math_block xml:space="preserve">\{ \emptyset,\ \{a\},\ \{b\},\ \{c\},\ \{a, b\},
\ \{a, c\},\ \{b, c\},\ \{a, b, c\}\}.</math_block><paragraph>A collection of elements with no order (like a set), but with
duplicate-valued elements is called a
<title_reference>bag</title_reference> <footnote_reference auto="1" ids="id1" refid="id2">1</footnote_reference>.
To distinguish bags from sets, we will use square brackets [] around
a bag's elements.
For example, bag [3, 4, 5, 4] is distinct from bag [3, 4, 5],
while set {3, 4, 5, 4} is indistinguishable from set
{3, 4, 5}.
However, bag [3, 4, 5, 4] is indistinguishable from bag
[3, 4, 4, 5].</paragraph><paragraph>A <title_reference>sequence</title_reference> is a collection of elements with an order, and
which may contain duplicate-valued elements.
A sequence is also sometimes called a <title_reference>tuple</title_reference> or a
<title_reference>vector</title_reference>.
In a sequence, there is a 0th element, a 1st element, 2nd element, and
so on.
We will use angle brackets <math>\langle\rangle</math> to enclose the
elements of a sequence.
For example, <math>\langle3, 4, 5, 4\rangle</math> is a sequence.
Note that sequence <math>\langle3, 5, 4, 4\rangle</math> is distinct from
sequence <math>\langle3, 4, 5, 4\rangle</math>, and both are distinct from
sequence <math>\langle3, 4, 5\rangle</math>.</paragraph><footnote auto="1" backrefs="id1" ids="id2" names="1"><label>1</label><paragraph>The object referred to here as a
bag is sometimes called a
<title_reference>multilist &lt;multilist&gt; &lt;Multilists&gt;</title_reference>.
But, the term multilist also refers to a list that may contain
sublists.</paragraph></footnote></section><section ids="relations" names="relations"><title>Relations</title><paragraph>A <title_reference>relation</title_reference> <math>R</math> over set <math>\mathbf{S}</math> is a set of
ordered pairs from <math>\mathbf{S}</math>.
As an example of a relation, if <math>\mathbf{S}</math> is
<math>\{a, b, c\}</math>, then</paragraph><math_block xml:space="preserve">\{ \langle a, c\rangle, \langle b, c\rangle, \langle c, b\rangle \}</math_block><paragraph>is a relation, and</paragraph><math_block xml:space="preserve">\{ \langle a, a\rangle, \langle a, c\rangle, \langle b, b\rangle,
\langle b, c\rangle, \langle c, c\rangle \}</math_block><paragraph>is a different relation.
If tuple <math>\langle x, y\rangle</math> is in relation <math>R</math>, we may
use the infix notation <math>xRy</math>.
We often use relations such as the less than operator (<math>&lt;</math>) on
the natural numbers, which includes ordered pairs such as
<math>\langle1, 3\rangle</math> and
<math>\langle2, 23\rangle</math>, but not <math>\langle3, 2\rangle</math> or
<math>\langle2, 2\rangle</math>.
Rather than writing the relationship in terms of ordered pairs, we
typically use an infix notation for such relations, writing <math>1&lt;3</math>.</paragraph><paragraph>Define the properties of relations as follows, with <math>R</math> a
binary relation over set <math>\mathbf{S}</math>.</paragraph><bullet_list bullet="*"><list_item><paragraph><math>R</math> is <title_reference>reflexive</title_reference> if <math>aRa</math>
for all <math>a \in \mathbf{S}</math>.</paragraph></list_item><list_item><paragraph><math>R</math> is <title_reference>irreflexive</title_reference> if <math>aRa</math> is not true
for all <math>a \in \mathbf{S}</math>.</paragraph></list_item><list_item><paragraph><math>R</math> is <title_reference>symmetric</title_reference> if whenever <math>aRb</math>,
then <math>bRa</math>, for all <math>a, b \in \mathbf{S}</math>.</paragraph></list_item><list_item><paragraph><math>R</math> is <title_reference>antisymmetric</title_reference> if whenever <math>aRb</math>
and <math>bRa</math>, then <math>a = b</math>, for all
<math>a, b \in \mathbf{S}</math>.</paragraph></list_item><list_item><paragraph><math>R</math> is <title_reference>transitive</title_reference> if whenever <math>aRb</math> and
<math>bRc</math>, then <math>aRc</math>, for all
<math>a, b, c \in \mathbf{S}</math>.</paragraph></list_item></bullet_list><paragraph>As examples, for the natural numbers, <math>&lt;</math> is
irreflexive (because :math`aRa` is never true),
antisymmetric (because there is no case where <math>aRb</math> and
<math>bRa</math>), and transitive.
Relation <math>\leq</math> is reflexive, antisymmetric, and transitive.
Relation <math>=</math> is reflexive, symmetric (and antisymmetric!),
and transitive.
For people, the relation "is a sibling of" is symmetric and
transitive.
If we define a person to be a sibling of himself, then it is
reflexive; if we define a person not to be a sibling of himself, then
it is not reflexive.</paragraph><raw format="xml" xml:space="preserve"><avembed
    type="ka"
    exer_name="SetTFrelation"
    long_name="SetTFrelation"
    points="1.0"
    required="True"
    threshold="5">
</avembed>
</raw></section><section ids="equivalence-relations" names="equivalence\ relations"><title>Equivalence Relations</title><paragraph><math>R</math> is an <title_reference>equivalence relation</title_reference> on set <math>\mathbf{S}</math>
if it is reflexive, symmetric, and transitive.
An equivalence relation can be used to partition a set into
<title_reference>equivalence classes &lt;equivalence class&gt;</title_reference>.
If two elements <math>a</math> and <math>b</math> are equivalent to each other,
we write <math>a \equiv b</math>.
A <title_reference>partition</title_reference> of a set <math>\mathbf{S}</math> is a collection of
subsets that are <title_reference>disjoint</title_reference> from each other and whose union is
<math>\mathbf{S}</math>.
An <title_reference>equivalence relation</title_reference> on set <math>\mathbf{S}</math> partitions
the set into disjoint subsets whose elements are equivalent.
The <title_reference>UNION/FIND &lt;UNION/FIND&gt; &lt;UnionFind&gt;</title_reference> algorithm efficiently
maintains equivalence classes on a set.
One application for such <title_reference>disjoint sets</title_reference>
computing a <title_reference>minimal cost spanning tree &lt;MCST&gt; &lt;MCST&gt;</title_reference>.</paragraph><topic><title>Example</title><paragraph>For the integers, <math>=</math> is an equivalence relation that
partitions each element into a distinct subset.
In other words, for any integer <math>a</math>, three things are true.</paragraph><enumerated_list enumtype="arabic" prefix="" suffix="."><list_item><paragraph><math>a = a</math>,</paragraph></list_item><list_item><paragraph>if <math>a = b</math> then <math>b = a</math>, and</paragraph></list_item><list_item><paragraph>if <math>a = b</math> and <math>b = c</math>, then <math>a = c</math>.</paragraph></list_item></enumerated_list><paragraph>Of course, for distinct integers <math>a</math>, <math>b</math>, and <math>c</math>
there are never cases where <math>a = b</math>, <math>b = a</math>, or
<math>b = c</math>.
So the requirements for symmetry and transitivity are never
violated, and therefore the relation is symmetric and transitive.</paragraph></topic><topic><title>Example</title><paragraph>If we clarify the definition of sibling to mean that a person is
a sibling of him- or herself, then the sibling relation is an
equivalence relation that partitions the set of people.</paragraph></topic><topic><title>Example</title><paragraph>We can use the <title_reference>modulus &lt;MiscMath&gt;</title_reference> function
to define an equivalence relation.
For the set of integers, use the modulus function
to define a binary relation such that two numbers
<math>x</math> and <math>y</math> are in the relation if and only if
<math>x \bmod m = y \bmod m</math>.
Thus, for <math>m = 4</math>, <math>\langle1, 5\rangle</math> is in the
relation because <math>1 \bmod 4 = 5 \bmod 4</math>.
We see that modulus used in this way defines an equivalence
relation on the integers, and this relation can be used to
partition the integers into <math>m</math> equivalence classes.
This relation is an equivalence relation because</paragraph><enumerated_list enumtype="arabic" prefix="" suffix="."><list_item><paragraph><math>x \bmod m = x \bmod m</math> for all <math>x</math>;</paragraph></list_item><list_item><paragraph>if <math>x \bmod m = y \bmod m</math>,
then <math>y \bmod m = x \bmod m</math>; and</paragraph></list_item><list_item><paragraph>if <math>x \bmod m = y \bmod m</math> and
<math>y \bmod m = z \bmod m</math>, then
<math>x \bmod m = z \bmod m</math>.</paragraph></list_item></enumerated_list></topic><raw format="xml" xml:space="preserve"><avembed
    type="ka"
    exer_name="SetTFequivrel"
    long_name="SetTFequivrel"
    points="1.0"
    required="True"
    threshold="5">
</avembed>
</raw></section><section ids="partial-orders" names="partial\ orders"><title>Partial Orders</title><paragraph>A binary relation is called a
<title_reference>partial order</title_reference> if it is antisymmetric and transitive.
If the relation is reflexive, it is called a
<title_reference>non-strict partial order</title_reference>.
If the relation is <title_reference>irreflexive</title_reference>, it is called a
<title_reference>strict partial order</title_reference>.
The set on which the partial order is defined is called a
<title_reference>partially ordered set</title_reference> or a <title_reference>poset</title_reference>.
Elements <math>x</math> and <math>y</math> of a set are <title_reference>comparable</title_reference> under
a given relation <math>R</math> if either <math>xRy</math> or <math>yRx</math>.
If every pair of distinct elements in a partial order are comparable,
then the order is called a <title_reference>total order</title_reference> or <title_reference>linear order</title_reference>.</paragraph><topic><title>Example</title><paragraph>For the integers, relations <math>&lt;</math> and <math>\leq</math> define
partial orders.
Operation <math>&lt;</math> is a total order because, for every pair of
integers <math>x</math> and <math>y</math> such that <math>x \neq y</math>,
either <math>x &lt; y</math> or <math>y &lt; x</math>.
Likewise, <math>\leq</math> is a total order because,
for every pair of integers <math>x</math> and <math>y</math> such that
<math>x \neq y</math>, either <math>x \leq y</math> or <math>y \leq x</math>.</paragraph></topic><topic><title>Example</title><paragraph>For the powerset of the integers, the subset
operator defines a partial order (because it is antisymmetric and
transitive).
For example, <math>\{1, 2\}\subseteq\{1, 2, 3\}</math>.
However, sets {1, 2} and {1, 3} are not comparable by the
subset operator, because neither is a subset of the other.
Therefore, the subset operator does not define a total order on the
powerset of the integers.</paragraph></topic><raw format="xml" xml:space="preserve"><avembed
    type="ka"
    exer_name="SetTFpartialorder"
    long_name="SetTFpartialorder"
    points="1.0"
    required="True"
    threshold="5">
</avembed>
</raw></section></document>