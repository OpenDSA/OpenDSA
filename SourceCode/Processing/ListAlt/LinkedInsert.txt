Code for LL deletion, assuming:
* curr points to the current node
* There is a trailer node

Object remove () {
  if (curr == tail) return null;
  Object temp = curr.val;
  curr.val = curr.next.val;
  if (curr.next == tail) tail == curr;
  curr.next = curr.next.next;
  return temp;
}
