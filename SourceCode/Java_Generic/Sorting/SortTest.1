import java.lang.reflect.Array;
import java.io.*;
import java.math.*;
import java.util.*;

public class SortTest<T extends Comparable<T>> {

  private T[] A;
  long time1, time2;
  int numruns = 5;
  int swaps;
  int compares;
  
  @SuppressWarnings("unchecked")
  public SortTest(Class<T> type, int size) {
        // This creates a truly reified arrays of the correct type at runtime
        this.A = (T[]) Array.newInstance(type, size);
  }
