version: '3.7'
services:
  opendsa:
    build:
      context: .
      dockerfile: Dockerfile
    image: opendsa:latest
    command: ["./scripts/start.sh"]
    volumes:
      - .:/opendsa
    ports:
      - "8000:8080"
